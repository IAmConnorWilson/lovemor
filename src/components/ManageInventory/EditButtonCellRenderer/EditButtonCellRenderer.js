import React, {Component} from 'react';

export default class EditButtonCellRenderer extends Component {

    render() {
        return (
            <button className="editItemBtn">Edit</button>
        );
    }
}